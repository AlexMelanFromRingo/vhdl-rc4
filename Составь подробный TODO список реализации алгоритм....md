# **TODO: Реализация криптографических алгоритмов RC4 и ГОСТ 28147-89**

Этот документ содержит пошаговое руководство, необходимые константы и тестовые данные для реализации алгоритмов с нуля.

## **Часть 1: RC4 (Rivest Cipher 4\)**

Тип: Потоковый шифр (Stream Cipher)  
Сложность: Низкая  
Ключевые особенности: Работает с байтами, переменная длина ключа.

### **1.1. Подготовка структур данных**

* \[ \] Создать массив состояния (S-box) `S` размером 256 байт (`unsigned char S\[256\])`.  
* \[ \] Подготовить массив ключа `Key` (длина от 1 до 256 байт).  
* \[ \] Подготовить переменные счетчиков `i` и `j` (обнулить перед началом).

### **1.2. Алгоритм KSA (Key-Scheduling Algorithm)**

Инициализация перестановки на основе ключа.

* \[ \] **Шаг 1:** Заполнить массив `S` линейно: `S\[i\] \= i` для `i` от 0 до 255\.  
* \[ \] **Шаг 2:** Перемешать массив `S` используя ключ:
```
  j \= 0;  
  for (i \= 0; i \< 256; i++) {  
      j \= (j \+ S\[i\] \+ Key\[i % key\_length\]) % 256;  
      swap(S\[i\], S\[j\]); // Поменять местами значения  
  }
```

### **1.3. Алгоритм PRGA (Pseudo-Random Generation Algorithm)**

Генерация гаммы (потока ключей) и шифрование.

* \[ \] Обнулить `i` и `j`.  
* \[ \] Для каждого байта входного текста `InputByte`:  
  1. `i \= (i \+ 1\) % 256  `
  2. `j \= (j \+ S\[i\]) % 256  `
  3. `swap(S\[i\], S\[j\])  `
  4. Получить байт гаммы `K`:  
     `t \= (S\[i\] \+ S\[j\]) % 256  `
     `K \= S\[t\]  `
  5. Зашифровать байт: `OutputByte \= InputByte XOR K`

### **1.4. Тестовые векторы RC4 (Проверка)**

Все значения в HEX.

**Test Vector 1:**

* **Key:** `Key` (ASCII) \-\> `4B 65 79  `
* **Plaintext:** `Plaintext` (ASCII) \-\> `50 6C 61 69 6E 74 65 78 74  `
* **Ciphertext:** `BB F3 16 E8 D9 40 AF 0A D3`

**Test Vector 2:**

* **Key:** `Wiki` (ASCII) \-\> `57 69 6B 69  `
* **Plaintext:** `pedia` (ASCII) \-\> `70 65 64 69 61  `
* **Ciphertext:** `10 21 BF 04 20`

## **Часть 2: ГОСТ 28147-89 (Магма)**

Тип: Блочный шифр (Block Cipher)  
Структура: Сеть Фейстеля  
Размер блока: 64 бита (8 байт)  
Размер ключа: 256 бит (32 байта)  
Количество раундов: 32

### **2.1. Подготовка и Константы (S-Boxes)**

В ГОСТе таблицы замен (S-блоки) являются параметром. Самый распространенный набор — **id-Gost28147-89-CryptoPro-A-ParamSet** (RFC 4357).

* \[ \] Реализовать таблицу замен `S\_BOX\[8\]\[16\]`.  
  Внимание: `S\[0\]` соответствует младшим 4 битам, `S\[7\]` — старшим.
```
// CryptoPro A-ParamSet  
byte S\_BOX\[8\]\[16\] \= {  
    {9, 6, 3, 2, 8, 11, 1, 7, 10, 4, 14, 15, 12, 0, 13, 5},  // S1 (мл. ниббл)  
    {3, 7, 14, 9, 8, 10, 15, 0, 5, 2, 6, 12, 11, 4, 13, 1},  // S2  
    {14, 4, 6, 2, 11, 3, 13, 8, 12, 15, 5, 10, 0, 7, 1, 9},  // S3  
    {14, 7, 10, 12, 13, 1, 3, 9, 0, 2, 11, 4, 15, 8, 5, 6},  // S4  
    {11, 5, 1, 9, 8, 13, 15, 0, 14, 4, 2, 3, 12, 7, 10, 6},  // S5  
    {3, 10, 13, 12, 1, 2, 0, 11, 7, 5, 9, 4, 8, 15, 14, 6},  // S6  
    {1, 13, 2, 9, 7, 10, 6, 0, 8, 12, 4, 5, 15, 3, 11, 14},  // S7  
    {11, 10, 15, 5, 0, 12, 14, 8, 6, 2, 3, 9, 1, 7, 13, 4}   // S8 (ст. ниббл)  
};
```

### **2.2. Работа с данными (Endianness)**

ГОСТ работает с данными в формате **Little-Endian** (младший байт по младшему адресу).

* \[ \] Функция конвертации `BytesToUInt32`: 4 байта \-\> 1 `uint32` (порядок: b0 b1 b2 b3 \-\> 0xb3b2b1b0).  
* \[ \] Функция конвертации `UInt32ToBytes`: обратно.  
* \[ \] Разбить 64-битный блок текста на две части: `N1` (младшая) и `N2` (старшая), обе типа `uint32`.  
* \[ \] Разбить 256-битный ключ на 8 подключей `X\[0\]...X\[7\]` по 32 бита.

### **2.3. Базовый цикл (Режим простой замены \- ECB)**

Шифрование одного 64-битного блока.

* \[ \] **Расписание ключей (Key Schedule):**  
  * Раунды 1–24: Использовать подключи `X\[0\] ... X\[7\]`, затем снова `X\[0\]...X\[7\]`, и еще раз (итого 3 прохода).  
    * Порядок: `0, 1, 2, 3, 4, 5, 6, 7` (повторить 3 раза).  
  * Раунды 25–32: Использовать подключи в обратном порядке.  
    * Порядок: `7, 6, 5, 4, 3, 2, 1, 0\`.  
* \[ \] Функция раунда:  
  Для каждого раунда `i` (от 0 до 31):  
  1. Взять текущий подключ `K\_curr`.  
  2. `Temp \= (N1 \+ K\_curr) mod 2^32` (Сложение по модулю 32 бит).  
  3. S-box замена:  
     Разбить `Temp` на 8 полубайт (4 бита). Каждый полубайт заменить значением из соответствующей строки `S\_BOX`.  
     Пример: `Value \= (S\_BOX\[7\]\[(Temp \>\> 28\) & 0xF\] \<\< 28\) | ... | S\_BOX\[0\]\[Temp & 0xF\]  `
  4. Циклический сдвиг: Сдвинуть результат влево на 11 бит.  
     `Result \= (Value \<\< 11\) | (Value \>\> 21\)  `
  5. Feistel Step:  
     `NewN1 \= N2 XOR Result  `
     `N2 \= N1  `
     `N1 \= NewN1  `
     (В последнем 32-м раунде N1 и N2 обычно не меняются местами, но результат записывается как N2|N1. Проверь конкретную реализацию: стандарт гласит, что после 32 раунда результат — (N2, N1), то есть фактически финальный своп происходит).

### **2.4. Тестовые векторы ГОСТ 28147-89 (ECB)**

Используем стандартный тестовый пример (Null Key, Null Plaintext для проверки таблиц, но лучше взять более сложный).

**Набор параметров:** CryptoPro-A (как указано выше).

**Test Vector 1:**

* **Key:** `00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  `
* **Plaintext:** `00 00 00 00 00 00 00 00  `
* **Ciphertext:** `?` (Зависит от S-box. Если использовать "Test S-box" из стандарта ГОСТ, результат будет: `42 AB B5 89 69 73 99 78\`. Для CryptoPro-A результат будет иным. Для отладки лучше использовать вектор ниже).

**Test Vector 2 (OpenSSL "gost89" default parameters):**

* **Key:** `KEY`: в ASCII, остальное нули \-\> `4B 45 59 3A 00 ... 00` (32 байта)  
* **Plaintext:** `00 00 00 00 00 00 00 00  `
* **Ожидаемый Ciphertext:** Нужно проверять под конкретную таблицу замен.

Рекомендуемый проверочный вектор (RFC 5830, Test param set):  
Если вы реализуете "Test Param Set" (другая таблица S-box, см. стандарт):

* **Key:** `E0`... (32 байта, все E0)  
* **Plaintext:** `00 00 00 00 00 00 00 00  `
* **Ciphertext:** `?`

*Совет: Для первой проверки реализуйте S-box из стандарта ГОСТ 28147-89 "Test Parameters":*
```
// Test Parameters S-Box (часто используется для проверки правильности алгоритма)  
byte S\_Test\[8\]\[16\] \= {  
    {4, 10, 9, 2, 13, 8, 0, 14, 6, 11, 1, 12, 7, 15, 5, 3},  
    {14, 11, 4, 12, 6, 13, 15, 10, 2, 3, 8, 1, 0, 7, 5, 9},  
    {5, 8, 1, 13, 10, 3, 4, 2, 14, 15, 12, 7, 6, 0, 9, 11},  
    {7, 13, 10, 1, 0, 8, 9, 15, 14, 4, 6, 12, 11, 2, 5, 3},  
    {6, 12, 7, 1, 5, 15, 13, 8, 4, 10, 9, 14, 0, 3, 11, 2},  
    {4, 11, 10, 0, 7, 2, 1, 13, 3, 6, 8, 5, 9, 12, 15, 14},  
    {13, 11, 4, 1, 3, 15, 5, 9, 0, 10, 14, 7, 6, 8, 2, 12},  
    {1, 15, 13, 0, 5, 7, 10, 4, 9, 2, 3, 14, 6, 11, 8, 12}  
};
```
При использовании **Test Parameters S-Box**:

* **Key:** Все нули.  
* **Plaintext:** Все нули.  
* **Ciphertext:** `CE AC 6C 0A 76 68 7D B2` (Little Endian bytes).

## **Чек-лист общих требований**

* \[ \] **Типы данных:** Использовать типы фиксированной ширины (`uint8\_t`, `uint32\_t`) из `\<stdint.h\>` (C/C++) или аналоги в других языках.  
* \[ \] **Модульная арифметика:** Убедиться, что сложение `N1 \+ Key` автоматически отбрасывает переполнение (работает как mod 2^32).  
* \[ \] **Padding (Дополнение):**  
  * RC4: Не требуется (потоковый).  
  * ГОСТ: Если данные не кратны 8 байтам, нужно дополнять (обычно PKCS\#5 или PKCS\#7 padding).  
* \[ \] **Режим сцепления (Chaining):**  
  * Для реализации полноценного шифрования файлов ГОСТом нужно реализовать режим **CFB** (Gamma) или **CNT** (Counter), так как ECB небезопасен для данных длиннее одного блока. Но для учебной задачи начните с ECB.

## **Полезные ссылки для отладки**

1. **CyberChef:** Отличный онлайн-инструмент.  
   * Для RC4 выберите "RC4".  
   * Для ГОСТ выберите "GOST" \-\> проверьте, какой S-box они используют по умолчанию (часто это CryptoPro или D-Test).  
2. **RFC 5830:** Описание ГОСТ 28147-89.  
3. **Википедия (eng):** Статья "GOST (block cipher)" содержит детальные диаграммы сети Фейстеля.
